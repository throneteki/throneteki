apiVersion: v1
kind: Service
metadata:
    name: throneteki-lobby
    # prettier-ignore
    namespace: {{ .Values.namespace }}
spec:
    selector:
        app: throneteki-lobby
    ports:
        - port: 80
          targetPort: 80
          protocol: TCP
